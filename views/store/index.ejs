<%- include('../partials/header') %>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
<%- include('../partials/navbar') %>
<!-- hero section -->
<%
const linePhotos = (registeredHomes && registeredHomes.length ? registeredHomes : [
  { houseName: "Sunset Villa",      location: "Goa",       photo: "/images/placeholder1.jpg" },
  { houseName: "Pine Cabin",        location: "Coorg",     photo: "/images/placeholder2.jpg" },
  { houseName: "The Glass House",   location: "Rishikesh", photo: "/images/placeholder3.jpg" },
  { houseName: "Hillside Cottage",  location: "Manali",    photo: "/images/placeholder4.jpg" },
  { houseName: "Bay House",         location: "Shillong",  photo: "/images/placeholder5.jpg" }
]).slice(0, 5);

const pegs = [
  { top: 0,   left: 117, accent: false },
  { top: 70,  left: 360, accent: false },
  { top: 112, left: 531, accent: true, big: true },
  { top: 70,  left: 702, accent: false },
  { top: 0,   left: 946, accent: false }
];

const frames = [
  { top: 30,  left: 24,  width: 211, height: 272, rotate: -8 },
  { top: 101, left: 253, width: 240, height: 304, rotate: -3 },
  { top: 146, left: 406, width: 275, height: 342, rotate: 1  },
  { top: 101, left: 595, width: 240, height: 304, rotate: 3  },
  { top: 30,  left: 853, width: 211, height: 272, rotate: 8  }
];
%>

<section class="pl-hero">
  <div class="pl-glow"></div>

  <div class="pl-leaf tl" aria-hidden="true">
    <svg viewBox="0 0 200 200" width="100%" xmlns="http://www.w3.org/2000/svg">
      <path d="M10 190 C20 120 40 70 100 30 C70 80 65 130 80 190 Z" fill="#20301a"/>
      <path d="M0 160 C30 130 55 100 70 60 C55 110 55 140 45 175 Z" fill="#1a2814"/>
      <path d="M40 190 C55 140 70 100 110 60 C90 110 92 150 100 190 Z" fill="#243a1c"/>
    </svg>
  </div>
  <div class="pl-leaf bl" aria-hidden="true">
    <svg viewBox="0 0 200 200" width="100%" xmlns="http://www.w3.org/2000/svg">
      <path d="M10 190 C20 120 40 70 100 30 C70 80 65 130 80 190 Z" fill="#20301a"/>
      <path d="M0 160 C30 130 55 100 70 60 C55 110 55 140 45 175 Z" fill="#1a2814"/>
      <path d="M40 190 C55 140 70 100 110 60 C90 110 92 150 100 190 Z" fill="#243a1c"/>
    </svg>
  </div>
  <div class="pl-leaf br" aria-hidden="true">
    <svg viewBox="0 0 200 200" width="100%" xmlns="http://www.w3.org/2000/svg">
      <path d="M190 190 C180 120 160 70 100 30 C130 80 135 130 120 190 Z" fill="#1c2b16"/>
      <path d="M200 160 C170 130 145 100 130 60 C145 110 145 140 155 175 Z" fill="#182412"/>
    </svg>
  </div>

  <div class="pl-eyebrow">
    <span class="n-pulse good"></span>
    41 STAYS, PHOTOGRAPHED AS THEY ARE
  </div>

  <h1 class="pl-title">See it,</h1>
  <h1 class="pl-title accent">before you book it.</h1>

  <p class="pl-sub">
    Every photo below is an actual stay in the collection, not a stock shot.
    Same room, same light, same view you'll wake up to.
  </p>

  <div class="pl-actions">
    <a href="/explore" class="pl-btn-primary">Browse all stays →</a>
    <a href="/host" class="pl-btn-ghost">List your place</a>
  </div>

  <div class="pl-line-wrap">
    <svg class="pl-rope-svg" viewBox="0 0 1090 300">
      <circle cx="13" cy="19" r="5" fill="#3a3a3a"/>
      <circle cx="1075" cy="19" r="5" fill="#3a3a3a"/>
      <path d="M13 19 Q74 27,130 40 Q256 74,373 109 Q460 134,544 152 Q628 134,715 109 Q802 74,958 40 Q1015 27,1075 19"
        fill="none" stroke="rgba(0,0,0,.4)" stroke-width="4" transform="translate(0,4)"/>
      <path d="M13 19 Q74 27,130 40 Q256 74,373 109 Q460 134,544 152 Q628 134,715 109 Q802 74,958 40 Q1015 27,1075 19"
        fill="none" stroke="#C9BBA0" stroke-width="3"/>
      <path d="M13 19 Q74 27,130 40 Q256 74,373 109 Q460 134,544 152 Q628 134,715 109 Q802 74,958 40 Q1015 27,1075 19"
        fill="none" stroke="#8f8065" stroke-width="1.2" stroke-dasharray="1 4"/>
    </svg>

<% pegs.forEach(p => { %>
    <div class="pl-peg <%= p.accent ? 'accent' : '' %>"
      style="top:<%= p.top %>px; left:<%= p.left %>px; <%= p.big ? 'width:30px;height:38px;' : '' %>"></div>
<% }) %>

<% linePhotos.forEach((home, i) => {
  const f = frames[i];
%>
    <div class="pl-photo"
      style="top:<%= f.top %>px; left:<%= f.left %>px; width:<%= f.width %>px; height:<%= f.height %>px;
             padding: 10px 10px 26px; --r:<%= f.rotate %>deg; transform: rotate(<%= f.rotate %>deg);
             animation-delay: <%= .3 + i * .1 %>s;">
      <img src="<%= home.photo %>"
        onerror="this.onerror=null;this.src='https://picsum.photos/seed/roovia<%= i %>/400/500';"
        alt="<%= home.houseName %>, <%= home.location %>"
        loading="lazy"
        style="height: calc(100% - 34px);">
      <div class="pl-photo-cap"><%= home.location %></div>
    </div>
<% }) %>
  </div>
</section>

  <!-- philosophy section -->
  <section class="philosophy-section">
    <div class="philosophy-label">
      WHY WE EXIST
    </div>
    <h2 class="philosophy-text">
      <span class="center-line">
        Travel should not pull 
      </span>
      <strong>you away from life.</strong> The right stay slows everything down —
      giving you space to <strong>reconnect with nature</strong>,
      with people, and with yourself. That's why every HomeStay is <strong>off-grid</strong> by design. Not a compromise — <strong>a choice.</strong>"
    </h2>
    <div class="world-inner">
      <div class="world-label">
        <span class="infinity" style="font-size: 25px;">∞</span> &nbsp; HOMES. 7 CONTINENTS. ALL CHOSEN BY HAND
      </div>
      <div class="world-content">
        <div class="world-map-area">
          <img
            src="/images/explore_world.png"
            alt="World Map"
            class="world-map">
          <svg class="travel-path" viewBox="0 0 1000 500" preserveAspectRatio="none">
            <!-- canada -> poland -->
            <path
              id="line1"
              class="travel-line"
              d="M190 160 C350 90, 470 120, 590 170"
            />
            <!-- poland -> australia -->
            <path
              id="line2"
              class="travel-line"
              d="M590 170 C760 240, 820 330, 860 420"
            />
            <!-- argentina -> canada -->
            <path
              id="line3"
              class="travel-line"
              d="M310 390 C180 330, 120 250, 190 160"
            />
            <!-- australia -> argentina -->
            <path
              id="line4"
              class="travel-line"
              d="M860 420 C720 470, 500 470, 310 390"
            />
          </svg>
          <div class="map-pin pin-1" id="pin1">
            <div class="pin-dot"></div>
            <div class="pin-card">
              <img src="/images/stay1.png">
              <div>
                <strong>Forest Cabin</strong>
                <span>Canada</span>
                <small>48.4284° N, 1.3656° W</small>
              </div>
            </div>
          </div>
          <div class="map-pin pin-2" id="pin2">
            <div class="pin-dot"></div>
            <div class="pin-card">
              <img src="/images/stay2.png">
              <div>
                <strong>Snow Home</strong>
                <span>Poland</span>
                <small>4.4284° N, 1.3656° W</small>
              </div>
            </div>
          </div>
          <div class="map-pin pin-3" id="pin3">
            <div class="pin-dot"></div>
            <div class="pin-card">
              <img src="/images/stay3.png">
              <div>
                <strong>Cliff House</strong>
                <span>Argentina</span>
                <small>37.7179° N, 8.5981° W</small>
              </div>
            </div>
          </div>
          <div class="map-pin pin-4" id="pin4">
            <div class="pin-dot"></div>
            <div class="pin-card">
              <img src="/images/stay4.png">
              <div>
                <strong>Beach Calm</strong>
                <span>Australia</span>
                <small>8.4095° S, 115.1889° E</small>
              </div>
            </div>
          </div>
        </div>
        <div class="world-copy">
          <h2>
            Discover stays
            shaped by
            <em>nature, silence,</em>
            and <em>slower living</em>.
          </h2>
          <p>
            We don't list homes. We adopt them. Each one passed a single test: did stepping inside make us exhale?
          </p>
          <a href="/explore" class="explore-btn">
            <span>EXPLORE STAYS</span>
            <div class="btn-arrow">
              →
            </div>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- listing section -->
  <main class="stays-showcase-section" id="listings">
    <div class="showcase-heading">
      <div>
        <span class="section-tag">THE COLLECTION</span>
        <h2>Stay close to what <em>matters.</em></h2>
        <p>Thoughfully chosen homes in the world's most peaceful places.<br>Each stay is unique, just like the journey you're on.</p>
      </div>
      <!-- <a href="/host/addHome" class="add-home-btn">
        + Add Your Home
      </a> -->
    </div>
    <div class="showcase-layout">
      <aside class="showcase-sidebar">
        <div class="sidebar-links">
          <a href="#listings" class="sidebar-item active">
            <span>🍃 All Stays</span>
            <small><%= registeredHomes.length %></small>
          </a>
          <% 
            const fallbackCategories = [
              { name: "Nature", icon: "🌲" },
              { name: "Water", icon: "🌊" },
              { name: "Mountains", icon: "⛰" },
              { name: "Remote", icon: "🏕" }
            ];
            const categoriesToRender = 
              (typeof categories !== "undefined" && categories.length)
              ? categories
              : fallbackCategories;
          %>
          <% categoriesToRender.forEach(category => { %>
            <a 
              href="#listings" 
              class="sidebar-item">
              <span>
                <%= category.icon || "🍂" %>
                <%= category.name %>
              </span>
              <small>
                <%= category.count || Math.floor(Math.random() * 20) + 4 %>
              </small>
            </a>
          <% }) %>
        </div>
        <div class="sidebar-note">
          <span>🍂 SLOWER LIVING</span>
          <p>
            Spaces that invite you to slow down
            and reconnect with yourself.
          </p>
        </div>
      </aside>
      <div class="stays-grid">
        <% registeredHomes.slice(0,8).forEach(home => { %>
        <a href="/homeList" class="stay-card-link">
          <div class="stay-card">
            <div class="card-image-wrap">
              <img src="<%= home.photo %>" alt="<%= home.houseName %>">
              <button class="wishlist-btn">♡</button>
            </div>
            <div class="stay-overlay">
              <div class="stay-location">
                <%= home.location %>
              </div>
              <h3 class="stay-title">
                <%= home.houseName %>
              </h3>
              <div class="stay-meta">
                <p class="stay-price">
                  ₹<%= home.price %>
                  <span>/ night</span>
                </p>
                <div class="stay-rating">
                  <span class="rating-star">★</span> 4.9
                </div>
              </div>
            </div>
          </div>
        </a>
        <% }) %>
      </div>
      <div class="discover-more-wrap">
        <a href="/homeList" class="discover-more-btn">
          <span>DISCOVER MORE</span>
          <div class="discover-arrow">→</div>
        </a>
      </div>
    </div>
  </main>

  <section class="cinematic-section">
    <!-- <div class="cinematic-top">
      <span class="cinematic-label">
        THE EXPERIENCE
      </span>
      <h2 class="cinematic-title">
        The best stays
        change your <em>pace.</em>
      </h2>
      <p class="cinematic-copy">
        Not designed to impress for a moment,
        but to stay with you long after you leave.
      </p>
    </div> -->
    <div class="cinematic-frame" id="cinematicFrame">
      <video
        id="experienceVideo"
        class="cinematic-video"
        autoplay
        muted
        loop
        playsinline>
        <source src="/videos/experience.mp4" type="video/mp4">
      </video>
      <div class="cinematic-overlay">
        <div class="overlay-top">
          <span>Slow Living</span>
          <span>Nature • Silence • Design</span>
        </div>
        <div class="overlay-center">
          <h3>
            Wake slowly.<br>
            Stay longer.
          </h3>
        </div>
        <div class="overlay-bottom">
          <button class="sound-btn" id="soundBtn">
            Enable Sound
          </button>
          <button class="fullscreen-btn" id="fullscreenBtn">
            Full Experience ↗
          </button>
        </div>
      </div>
    </div>
  </section>

  <section class="testimonials-section">
    <div class="testimonials-top">
      <span class="testimonials-label">
        GUEST EXPERIENCES
      </span>
      <h2>
        What people <em>feel</em>
        after staying
      </h2>
      <p>
        Real experiences from guests who found
        more than just a place to stay.
      </p>
    </div>
    <div class="testimonial-slider">
      <div class="testimonial-track">
        <div class="testimonial-card">
          <p class="testimonial-quote">
            “The moment I walked in,
            it felt like the space understood me.
            Quiet, minimal, and deeply calming.”
          </p>
          <div class="testimonial-user">
            <img src="https://i.pravatar.cc/100?img=32">
            <div>
              <h4>Riya Sharma</h4>
              <span>Delhi</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <p class="testimonial-quote">
            “Not just a stay —
            it felt like a reset.
            Everything from light to textures
            was thoughtfully done.”
          </p>
          <div class="testimonial-user">
            <img src="https://i.pravatar.cc/100?img=12">
            <div>
              <h4>Aman Verma</h4>
              <span>Bangalore</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <p class="testimonial-quote">
            “I've stayed in many places,
            but this one had a soul.
            You can feel the intention behind it.”
          </p>
          <div class="testimonial-user">
            <img src="https://i.pravatar.cc/100?img=45">
            <div>
              <h4>Neha Kapoor</h4>
              <span>Mumbai</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <p class="testimonial-quote">
            “The silence here
            felt expensive in the best way.”
          </p>
          <div class="testimonial-user">
            <img src="https://i.pravatar.cc/100?img=22">
            <div>
              <h4>Arjun Mehta</h4>
              <span>Pune</span>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <p class="testimonial-quote">
            “We booked for two nights
            and ended up extending our stay.
            That says everything.”
          </p>
          <div class="testimonial-user">
            <img src="https://i.pravatar.cc/100?img=55">
            <div>
              <h4>Sana Khan</h4>
              <span>Hyderabad</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <%- include('../partials/footer') %>
  </body>

  <script>
    document.querySelector('.scroll-indicator').addEventListener('click', () => {
      const nextSection = document.querySelector('#next-section') || document.querySelector('#listings');
      if (nextSection) nextSection.scrollIntoView({ behavior: 'smooth' });
    });
    window.addEventListener('scroll', () => {
      const scrollEl = document.querySelector('.scroll-indicator');
      const scrollY = window.scrollY;

      if (scrollY > 100) {
        scrollEl.style.opacity = '0';
        scrollEl.style.pointerEvents = 'none';
      } else {
        scrollEl.style.opacity = '1';
        scrollEl.style.pointerEvents = 'auto';
      }
    });
    const scrollEl = document.querySelector('.scroll-indicator');
    window.addEventListener('scroll', () => {
      let y = window.scrollY;
      scrollEl.style.opacity = 1 - Math.min(y / 150, 1);
    });
    const cta   = document.getElementById('heroCta');
    const icon  = cta.querySelector('.cta-icon');
    const label = cta.querySelector('.cta-label');
    cta.addEventListener('mouseenter', () => {
      const pillW    = cta.offsetWidth;
      const iconW    = icon.offsetWidth;
      const labelW   = label.offsetWidth;
      const leftPad  = 24;
      const rightPad = 6;
      const iconTravel = -(pillW - rightPad - iconW - leftPad+8);
      const labelTravel =leftPad+11;
      icon.style.transform  = `translateX(${iconTravel}px)`;
      icon.style.background = '#c4a96b';
      icon.style.color      = '#1a1a14';
      label.style.transform = `translateX(${labelTravel}px)`;
      cta.style.transform   = 'scale(1.4)';
    });
    cta.addEventListener('mousemove', (e) => {
      const rect = cta.getBoundingClientRect();
      const x = (e.clientX - rect.left - rect.width / 2) / rect.width;
      const y = (e.clientY - rect.top  - rect.height / 2) / rect.height;
      cta.style.transform = `translate(${x * 6}px, ${y * 4}px) scale(1.4)`;
    });
    cta.addEventListener('mouseleave', () => {
      icon.style.transform  = 'translateX(0)';
      icon.style.background = '#1a1a14';
      icon.style.color      = '#F0EDE6';
      label.style.transform = 'translateX(0)';
      label.style.opacity   = '1';
      cta.style.transform   = 'translate(0,0) scale(1)';
    });
    // Live badge — fluctuates naturally between 18–34
    (function () {
      const el = document.getElementById('liveCount');
      if (!el) return;
      let count = 23;
      setInterval(() => {
        const delta = Math.floor(Math.random() * 3) - 1; // -1, 0, or +1
        count = Math.min(34, Math.max(18, count + delta));
        el.textContent = count;
      }, 4000);
    })();
    const animations = [
      {
        line: document.getElementById("line1"),
        pins: ["pin1", "pin2"]
      },
      {
        line: document.getElementById("line2"),
        pins: ["pin2", "pin4"]
      },
      {
        line: document.getElementById("line3"),
        pins: ["pin3", "pin1"]
      },
      {
        line: document.getElementById("line4"),
        pins: ["pin4", "pin3"]
      }
    ];
    function runRandomAnimation(){
      document.querySelectorAll('.travel-line')
        .forEach(line => line.classList.remove('active'));
      document.querySelectorAll('.map-pin')
        .forEach(pin => pin.classList.remove('active'));
      const random =
        animations[Math.floor(Math.random() * animations.length)];
      random.line.classList.add('active');
      random.pins.forEach(id => {
        document.getElementById(id).classList.add('active');
      });
      setTimeout(() => {
        random.line.classList.remove('active');
        random.pins.forEach(id => {
          document.getElementById(id).classList.remove('active');
        });
      }, 3500);
    }
    runRandomAnimation();
    setInterval(runRandomAnimation, 4500);

    const video = document.getElementById("experienceVideo");
    const cinematicFrame =
    document.getElementById("cinematicFrame");
    const soundBtn =
    document.getElementById("soundBtn");
    const fullscreenBtn =
    document.getElementById("fullscreenBtn");
    cinematicFrame.addEventListener("mouseenter", () => {
      video.muted = false;
    });
    cinematicFrame.addEventListener("mouseleave", () => {
      video.muted = true;

    });
    soundBtn.addEventListener("click", () => {
      video.muted = !video.muted;
      soundBtn.innerText =
        video.muted
        ? "Enable Sound"
        : "Sound On";
    });
    fullscreenBtn.addEventListener("click", async () => {
      if(video.requestFullscreen){
        await video.requestFullscreen();
      }
      video.muted = false;
      soundBtn.innerText = "Sound On";
    });
    const testimonialTrack =
    document.querySelector(".testimonial-track");
    let reversed = false;
    setInterval(() => {
      reversed = !reversed;
      if(reversed){
        testimonialTrack.classList.add("reverse");
      } else {
        testimonialTrack.classList.remove("reverse");
      }
    }, 32000);
    
  </script>
</html>